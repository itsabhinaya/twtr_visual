extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href='/stats') stats

  form(name="twi_user", action='/stats', method="POST")
        div.input
            span.label Username
            input(type="text", name="user")
        div.actions
            input(type="submit", value="Generate")
